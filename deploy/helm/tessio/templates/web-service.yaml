apiVersion: v1
kind: Service
metadata:
  name: {{ include "tessio.fullname" . }}-web
  labels: {{- include "tessio.labels" . | nindent 4 }}
spec:
  selector: {{- include "tessio.componentSelectorLabels" (dict "ctx" . "component" "web") | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
